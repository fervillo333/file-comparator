if __name__ == "__main__":
    # 1. Визначаємо назви файлів, які хочемо порівняти
    input_file_1 = "file1.txt"
    input_file_2 = "file2.txt"
    
    # 2. Створюємо екземпляр керуючого класу (менеджера)
    # Передаємо йому назви файлів
    manager = ComparisonManager(input_file_1, input_file_2)
    
    # 3. Викликаємо головний метод, який запускає весь ланцюжок:
    # Читання -> Порівняння -> Запис результатів
    manager.run()